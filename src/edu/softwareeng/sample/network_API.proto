syntax = "proto3";

package edu.softwareeng.sample;


service ComputeService {
    rpc performComputation (ComputationRequest) returns (ComputationResponse);
}


message ComputationRequest {
    repeated string numbers = 1;  // List of numbers for computation
}


message ComputationResponse {
    string result = 1;  // Computation result
}
